# Company-Project-3

# Riverside Community Hub Platform

## Setup
1. Clone repo
2. Install dependencies in `backend` and `frontend`
3. Add `.env` with Supabase URL + Keys
4. Run backend: `npm run dev`
5. Run frontend: `npm start`

## Deployment
- Backend → Vercel
- Frontend → Vercel
- Database → Supabase
